name: NexoWar
version: '1.0-SNAPSHOT'
main: me.nexo.war.NexoWar
description: 'Sistema de Guerras de Honor entre clanes.'
author: 'Z0101'
# 🌟 FIX: Aplicado el estándar exacto de la documentación de Paper
api-version: '26.1.2'

dependencies:
  server:
    # 📌 DEPENDENCIAS FUERTES (Obligatorias)
    NexoCore:
      load: BEFORE
      required: true
      join-classpath: true
    NexoClans:
      load: BEFORE
      required: true
      join-classpath: true
    NexoEconomy:
      load: BEFORE
      required: true
      join-classpath: true
    NexoProtections:
      load: BEFORE
      required: true
      join-classpath: true

# 💡 NOTA: El comando /war ha sido eliminado del YAML.
# El framework Lamp lo registrará de forma nativa en tu código Java.
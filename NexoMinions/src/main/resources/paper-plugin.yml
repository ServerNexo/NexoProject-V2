name: NexoMinions
version: '${project.version}'
main: me.nexo.minions.NexoMinions
description: 'Sistema de minions y automatizacion.'
author: 'TuNombre'
# 🌟 FIX: Aplicado el estándar exacto de la documentación de Paper
api-version: '26.1.2'

dependencies:
  server:
    # 📌 DEPENDENCIAS FUERTES (Obligatorias)
    NexoCore:
      load: BEFORE
      required: true
      join-classpath: true
    Nexo:
      load: BEFORE
      required: true
      join-classpath: true
    AuraSkills:
      load: BEFORE
      required: true
      join-classpath: true
    NexoColecciones:
      load: BEFORE
      required: true
      join-classpath: true
    NexoProtections:
      load: BEFORE
      required: true
      join-classpath: true

# 💡 NOTA: El comando /minion ha sido eliminado del YAML.
# El framework Lamp lo inyectará directamente desde tu código Java en tiempo de ejecución.